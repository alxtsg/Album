{
  "name": "album",
  "version": "1.0.2",
  "author": "alextsang@live.com",
  "description": "Static album page generation tool.",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@bitbucket.org/alxtsg/journey.git"
  },
  "license": "BSD-3-Clause",
  "homepage": "https://bitbucket.org/alxtsg/journey#readme",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist/",
    "copy-env": "copyfiles -u 1 src/.env.template dist",
    "copy-package-json": "copyfiles package*.json dist",
    "build": "tsc && npm run copy-env && npm run copy-package-json",
    "test": "mocha --recursive tests/**/*.test.js"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "mustache": "^4.2.0"
  },
  "devDependencies": {
    "@types/mocha": "^8.2.2",
    "@types/node": "^14.14.41",
    "copyfiles": "^2.4.1",
    "mocha": "^8.3.2",
    "rimraf": "^3.0.2",
    "typescript": "^4.2.4"
  }
}
